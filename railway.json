{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node_modules/.bin/prisma db push --skip-generate --accept-data-loss 2>&1 || true; node_modules/.bin/tsx prisma/seed.ts 2>&1 || echo 'Seed failed - use /api/seed'; npm start",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
